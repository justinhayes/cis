#https://wiki.cloudera.com/display/~mbrooks/Simple+Cloudera+Search+Example

export CIS_HOME=/root/cis/
export SOLR_CONF_HOME=$CIS_HOME/conf/solrconf
export SOLR_HOME=/opt/cloudera/parcels/SOLR/lib/solr

solrctl --zk hadoop0:2181/solr instancedir --generate $SOLR_CONF_HOME

#now update $SOLR_CONF_HOME/conf/schema.xml

cp $CIS_HOME/conf/log4j.properties $SOLR_CONF_HOME

solrctl --zk hadoop0:2181/solr collection --create ciscollection
